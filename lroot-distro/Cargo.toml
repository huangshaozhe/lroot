[package]
name = "lroot-distro"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "rustls-tls"], default-features = false }
tar = "0.4"
flate2 = "1"

[dev-dependencies]
tempfile = "3"
